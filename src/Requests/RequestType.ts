enum RequestType {
  PING = 'ping',
  SERVER_TIME = 'serverTime',
  EXCHANGE_INFO = 'exchangeInfo',
  ORDER_BOOK = 'orderBook',
  NEW_ORDER = 'newOrder',
  NEW_ORDER_TEST = 'newOrderTest'
}

export default RequestType;